//Compatibility or polyfill functions.

String.prototype.replaceAt=function(index:number, character:string):string {
    return this.substr(0, index) + character + this.substr(index+character.length);
}

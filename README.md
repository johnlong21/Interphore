The history has been altered to remove git lfs, because it made it impossible to actually download or upload anything.

This version uses CMake to build:
```bash
mkdir _build && cd _build
cmake ..
make
```
